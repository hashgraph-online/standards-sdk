name: Deprecated Manual Canary Publish Entry Point

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Use publish.yml instead
        run: |
          echo "Manual canary publishing now runs through .github/workflows/publish.yml so npm trusted publishing matches the configured workflow filename."
