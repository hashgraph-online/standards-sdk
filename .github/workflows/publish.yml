name: Publish Standards SDK

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: standards-sdk-publish-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  publish:
    name: Release SDK packages
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm -r run build

      - name: Bump version
        id: version
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "current=$CURRENT_VERSION" >> "$GITHUB_OUTPUT"

          pnpm version patch --no-git-tag-version

          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "new=$NEW_VERSION" >> "$GITHUB_OUTPUT"

          pnpm --filter @hol-org/standards-sdk version "$NEW_VERSION" --no-git-tag-version
          pnpm --filter @hol-org/rb-client version patch --no-git-tag-version

      - name: Check if version already published
        id: version_check
        run: |
          NEW_VERSION="${{ steps.version.outputs.new }}"
          if npm view @hashgraphonline/standards-sdk@$NEW_VERSION version >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Version $NEW_VERSION already exists on npm"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Version $NEW_VERSION is new, will publish"
          fi

      - name: Publish @hashgraphonline/standards-sdk
        if: steps.version_check.outputs.exists == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm publish --access public --no-git-checks --provenance

      - name: Publish @hol-org/standards-sdk
        if: steps.version_check.outputs.exists == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm --filter @hol-org/standards-sdk publish --access public --no-git-checks --provenance

      - name: Publish @hol-org/rb-client
        if: steps.version_check.outputs.exists == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm --filter @hol-org/rb-client publish --access public --no-git-checks --provenance

      - name: Publish to JSR
        if: steps.version_check.outputs.exists == 'false'
        run: |
          # Update jsr.json version to match
          NEW_VERSION="${{ steps.version.outputs.new }}"
          jq --arg v "$NEW_VERSION" '.version = $v' jsr.json > jsr.json.tmp && mv jsr.json.tmp jsr.json
          npx jsr publish --allow-dirty --allow-slow-types

      - name: Create Git tag
        if: steps.version_check.outputs.exists == 'false'
        run: |
          git tag "v${{ steps.version.outputs.new }}"
          git push --tags

      - name: Skip publish (version already exists)
        if: steps.version_check.outputs.exists == 'true'
        run: echo "Version ${{ steps.version.outputs.new }} already exists on npm; skipping publish."
